DIR=/ccs/home/syamamoto/tmp/src/Grid_cleanedup_for_pullrequest/systems/Frontier

libs=`$DIR/grid-config --libs`
ldflags=`$DIR/grid-config --ldflags`

cxxflags=`$DIR/grid-config --cxxflags`
≈ƒcxxflags_add=`$DIR/grid-config --cxx | awk '{print $2}'`
cxx=`$DIR/grid-config --cxx | awk '{print $1}'`

echo $libs
echo
echo $cxxflags
echo
echo $cxxflags_add
echo
echo $cxx

mkdir -p build
cd build
pwd
export LDFLAGS="$ldflags -L$DIR/Grid/ $libs"
echo $LDFLAGS

cmake .. --fresh -DCMAKE_CXX_COMPILER=$cxx -DCMAKE_CXX_FLAGS="$cxxflags $cxxflags_add -I/ccs/home/syamamoto/tmp/src/Grid_cleanedup_for_pullrequest/ -I$DIR/Grid/ "
#--fresh
